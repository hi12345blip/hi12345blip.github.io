body {
  background: #0f0f0f;
  color: white;
  font-family: Arial;
}

button {
  background: purple;
  border: none;
  border-radius: 12px;
  padding: 12px 20px;
  color: white;
  transition: 0.3s;
}

button:hover {
  transform: scale(1.1);
  box-shadow: 0 0 20px purple;
}
